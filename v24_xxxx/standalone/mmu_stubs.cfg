#####################################################################
#   MMU command stubs (standalone mode)
#####################################################################

[gcode_macro MMU]
description: No-op stub. No MMU is configured in standalone mode.
gcode:
    {% if params.ENABLE is defined and params.ENABLE|int == 1 %}
        RESPOND TYPE=error MSG="MMU ENABLE=1 ignored: printer.cfg is in standalone mode. Switch to mode_mmu.cfg."
    {% endif %}

[gcode_macro MMU_START_SETUP]
description: No-op stub for standalone mode
gcode:

[gcode_macro MMU_START_CHECK]
description: No-op stub for standalone mode
gcode:

[gcode_macro MMU_START_LOAD_INITIAL_TOOL]
description: No-op stub for standalone mode
gcode:

[gcode_macro MMU_END]
description: No-op stub for standalone mode
gcode:

[gcode_macro _MMU_UPDATE_HEIGHT]
description: No-op stub for standalone mode
gcode:
